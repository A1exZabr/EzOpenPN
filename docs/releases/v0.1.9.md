# EzOpenPN v0.1.9: предварительный выпуск

Личный сервер защищённых подключений с русской панелью, двумя независимыми транспортами и установкой одной командой. Первая рекомендация: отдельный чистый VPS без сайтов и других служб.

## Начать

Откройте [инструкцию с нуля](https://github.com/A1exZabr/EzOpenPN/blob/main/docs/getting-started.md). Она объясняет выбор сервера, вход в консоль, установку, создание первого профиля и проверку результата.

Этот исторический выпуск использует контейнеры из прежнего реестра Forgejo. На 16 сентября реестр недоступен, поэтому новую установку v0.1.9 не рекомендуем. Перенос в GitHub выполняется новой версией: подписанные файлы v0.1.9 не изменяются. Проверенную актуальную команду смотрите в [README](../../README.md#установка-одной-командой).

Уже работающую установку из-за переноса канала переустанавливать не нужно. Сохраняйте профили и резервные копии; порядок обновления будет указан вместе с новым кандидатом.

## Что входит

- VLESS + Reality поверх XHTTP и независимый Hysteria2 с Salamander.
- Веб-панель, отдельные профили устройств, общая ссылка и QR-код, отключение профиля.
- Автоматическая настройка Docker, сервисов, сертификата и поддерживаемого локального firewall.
- Диагностика, резервные копии, восстановление и сброс пароля панели через `sudo ezopenpn admin reset-password`.
- Минимум 512 МиБ общей RAM, видимой ОС, без обязательного swap; рекомендуется тариф от 1 ГиБ RAM. Нужно 4 ГиБ свободного диска и собственный публичный IPv4.

## Что проверено

Неизменяемый подписанный тег `v0.1.9` указывает на commit `e3067621ce7bb72330ff0aa93dda4f707ef5ecf3`. Опубликованы тот же пакет, контрольные суммы, Sigstore-подписи и SPDX SBOM; документация в `main` может быть новее кода тега.

Для этого commit успешно завершились:

- [CI](https://github.com/A1exZabr/EzOpenPN/actions/runs/33966946319), включая тесты панели, shell и контейнеров;
- [CodeQL](https://github.com/A1exZabr/EzOpenPN/actions/runs/33966946303);
- [сборка и подписи пяти образов](https://github.com/A1exZabr/EzOpenPN/actions/runs/33966959397);
- [VM Matrix](https://github.com/A1exZabr/EzOpenPN/actions/runs/33967284843): Ubuntu 22.04/24.04 и Debian 12/13, 1 ГиБ RAM без swap;
- [подписанный Candidate release](https://github.com/A1exZabr/EzOpenPN/actions/runs/33967355827).

Отдельно пользователь самостоятельно установил v0.1.9 на своём VPS: 891 МиБ общей RAM, без swap, пять сервисов healthy. Docker и Compose были предустановлены. В предоставленном статусе профили ещё не созданы; этот лог не подтверждает клиентскую матрицу. Адрес сервера и административные данные не публикуются.

## Ограничения

Это **pre-release**, не полностью проверенный стабильный выпуск. Ещё не завершены реальные испытания обоих транспортов в фиксированной и мобильной сети, активного отзыва подключений и точных версий Android/iOS/Windows-клиентов. Серверные healthcheck и лабораторная VM-матрица их не заменяют.

Поддерживаются amd64/x86_64, Ubuntu 22.04/24.04 и Debian 12/13. ARM и общий IP за NAT не поддерживаются. Внешний firewall настраивается у провайдера: `80/tcp`, `443/tcp`, `443/udp`, `9443/tcp`; правило SSH сохраняется. Автоматическая совместимость со всеми приложениями и работа при любых блокировках не обещаются.

## Безопасность и помощь

- [SSH-ключи, отключение паролей и аварийное восстановление](https://github.com/A1exZabr/EzOpenPN/blob/main/docs/ssh-security.md).
- [Если доступ пропал: проверки и переезд на другой IP](https://github.com/A1exZabr/EzOpenPN/blob/main/docs/server-migration.md). Сначала проверяется новый сервер, затем отменяется старая аренда. Смена IP не является универсальным решением.
- [Обычные ошибки](https://github.com/A1exZabr/EzOpenPN/issues): сообщайте версию и код ошибки, без ссылок профилей, QR-кодов, логинов, IP и ключей.
- [Уязвимости: только закрытая форма](https://github.com/A1exZabr/EzOpenPN/security/advisories/new).
